body {
  margin: 0;
  background: #fff;
  font-family: Arial, sans-serif;
}

.page {
  max-width: 1080px;
  margin: 0 auto;
  padding: 40px 50px;
  text-align: center;
}

/* ===== ОБЛАКА ===== */
.cloud {
  display: inline-block;
  padding: 14px 26px;
  margin: 24px auto;
  font-family: "Monotype Corsiva", cursive;
  font-size: 12px;
  color: #b08a6a;
  background: #fff;
  border: 2px solid #000;
  border-radius: 40px;
  position: relative;
}

.cloud::before,
.cloud::after {
  content: "";
  position: absolute;
  bottom: -8px;
  width: 12px;
  height: 12px;
  background: #fff;
  border: 2px solid #000;
  border-radius: 50%;
}

.cloud::before {
  left: 20px;
}

.cloud::after {
  left: 36px;
}

.cloud.title {
  font-size: 22px;
}

/* ===== ПОДЗАГОЛОВОК ===== */
.subtitle {
  font-size: 14px;
  margin-bottom: 30px;
}

/* ===== ФОТОРАМКА ===== */
.photo-frame {
  position: relative;
  margin: 30px auto;
  padding: 6px;
  border: 2px solid #000;
  border-radius: 20px;
  background: #fff;
  max-width: 980px;
}

.photo-frame.small {
  max-width: 480px;
}

.photo-frame img {
  width: 100%;
  display: block;
  border-radius: 14px;
  filter: blur(14px);
  transition: filter 0.4s ease;
}

.photo-frame:not(.blur) img {
  filter: blur(0);
}

/* ===== ЗАГЛУШКА ===== */
.overlay {
  position: absolute;
  inset: 6px;
  border-radius: 14px;
  background: rgba(255,255,255,0.6);
  cursor: pointer;
}

.photo-frame:not(.blur) .overlay {
  display: none;
}

/* ===== РЯД ===== */
.row {
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 20px;
  align-items: center;
  margin: 40px 0;
}

.left-text {
  font-size: 12px;
  text-align: left;
}

/* ===== МОБИЛКА ===== */
@media (max-width: 768px) {
  .page {
    padding: 20px;
  }

  .row {
    grid-template-columns: 1fr;
    text-align: center;
  }

  .left-text {
    text-align: center;
  }
}
