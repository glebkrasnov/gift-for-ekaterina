body {
    margin: 0;
    padding: 0;
    font-family: Arial, sans-serif;

    background: url("bg14f.png") no-repeat center center fixed;
    background-size: cover;
}

/* Центр экрана */
.page {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
}

/* Экран входа */
#login.container {
    width: 100%;
    max-width: 360px;
    padding: 16px;
    background: rgba(255,255,255,0.95);
    border-radius: 12px;

    display: flex;
    flex-direction: column;
    gap: 12px;
}

.center-text {
    text-align: center;
}

input {
    padding: 12px;
    font-size: 16px;
}

button,
.gift-btn {
    padding: 14px;
    font-size: 16px;
    border-radius: 8px;
    border: none;
    background: #18171C;
    color: #fff;
    text-decoration: none;
    text-align: center;
    cursor: pointer;
}

button:hover,
.gift-btn:hover {
    background: #A5A5A5;
}

.error {
    color: red;
    text-align: center;
}

/* Контейнер приветствия */
.welcome-box {
    background: rgba(255,255,255,0.85);
    padding: 20px 26px;
    border-radius: 16px;
    margin-bottom: 20px;
    opacity: 0;
    animation: fadeIn 1s forwards;
}

/* Приветствие */
.welcome {
    text-align: center;
    color: #000;
    font-size: 24px;
    font-weight: bold;
    line-height: 1.3;
}

.welcome .name {
    font-size: 30px;
}

/* Белый контейнер */
.content-box {
    background: rgba(255,255,255,0.95);
    padding: 22px;
    border-radius: 16px;
    text-align: center;
    max-width: 360px;
    width: 90%;
    margin-bottom: 20px;
}

/* Текст и видео */
.text,
#media,
.gift-btn {
    opacity: 0;
}

.text {
    margin-top: 10px;
}

.text.small {
    font-size: 16px;
    margin-top: 16px;
}

#media {
    margin-top: 10px;
}

#media video {
    width: 100%;
    border-radius: 12px;
}

/* Анимации */
.fade-in {
    animation: fadeIn 1s forwards;
}

.fade-out {
    animation: fadeOut 1s forwards;
}

@keyframes fadeIn {
    to { opacity: 1; }
}

@keyframes fadeOut {
    to { opacity: 0; }
}

.hidden {
    display: none;
}

/* Мобилки */
@media (max-width: 480px) {
    .welcome {
        font-size: 24px;
    }

    .welcome .name {
        font-size: 28px;
    }
}


