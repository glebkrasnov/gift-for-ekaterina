/* ==== НАСТРОЙКИ ==== */
:root {
  --page-width: 1080px;
  --side-padding: 50px;
  --cloud-color: #c8a47e;
}

/* ==== БАЗА ==== */
* {
  box-sizing: border-box;
}

body {
  margin: 0;
  background: #f5f5f5; /* ← ЗДЕСЬ ПОТОМ СВОЙ ФОН */
  font-family: Arial, sans-serif;
}

/* ==== КОНТЕЙНЕР ==== */
.page {
  max-width: var(--page-width);
  margin: 0 auto;
  padding: 40px var(--side-padding);
}

/* ==== ТЕКСТ ==== */
.text {
  margin: 16px 0;
}

.text.normal {
  font-size: 12px;
  color: #000;
}

.text.bold {
  font-weight: bold;
}

.text.small {
  font-size: 10px;
}

.text.big {
  font-size: 20px;
}

/* ==== ОБЛАКО ==== */
.cloud {
  display: inline-block;
  background: #fff;
  border-radius: 30px;
  padding: 14px 20px;
  margin: 20px 0;
  font-family: "Monotype Corsiva", cursive;
  font-size: 12px;
  color: var(--cloud-color);
  box-shadow: 0 4px 10px rgba(0,0,0,0.08);
}

.cloud.title {
  font-size: 22px;
  text-transform: uppercase;
}

/* ==== МЕДИА ==== */
.media {
  position: relative;
  margin: 24px 0;
  border-radius: 16px;
  overflow: hidden;
}

.media img,
.media video {
  width: 100%;
  display: block;
  border-radius: 16px;
  filter: blur(12px);
  transition: filter 0.4s ease;
}

.media.blur img,
.media.blur video {
  filter: blur(12px);
}

.media:not(.blur) img,
.media:not(.blur) video {
  filter: blur(0);
}

/* ==== РАЗМЕРЫ ==== */
.media.small { max-width: 500px; }
.media.tiny { max-width: 400px; }

/* ==== ЗАГЛУШКА ==== */
.overlay {
  position: absolute;
  inset: 0;
  background: rgba(0,0,0,0.35);
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fff;
  font-size: 18px;
  cursor: pointer;
}

.media:not(.blur) .overlay {
  display: none;
}

/* ==== РЯД ==== */
.row.split {
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 20px;
  align-items: center;
}

/* ==== ОБВОДКА ЦИФРЫ ==== */
.circle {
  display: inline-block;
  padding: 2px 6px;
  border: 1px solid #000;
  border-radius: 50%;
  font-size: 9px;
}

/* ==== МОБИЛКА ==== */
@media (max-width: 768px) {
  .page {
    padding: 30px 20px;
  }

  .row.split {
    grid-template-columns: 1fr;
  }
}
